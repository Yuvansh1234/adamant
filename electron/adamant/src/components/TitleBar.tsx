/**
 * Fills the space reserved by the frameless macOS title bar. The `app-region`
 * style makes the strip draggable like a native chrome bar.
 */
export function TitleBar() {
  return <header className="titlebar">adamant</header>
}
