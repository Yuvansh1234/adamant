@AGENTS.md

## Claude Code

- Skills in `.claude/skills/` are generated from `.agents/skills/` by `pnpm skills:sync`. Edit the
  `.agents/skills/` copy, then sync; `pnpm skills:check` fails if they differ.
