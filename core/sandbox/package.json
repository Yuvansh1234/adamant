{
  "name": "@adamant/sandbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Docker sandbox runner for isolated candidate patch validation",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test tests/**/*.test.ts"
  },
  "keywords": [
    "sandbox",
    "docker",
    "testing",
    "adamant"
  ]
}
